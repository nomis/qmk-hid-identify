Usage
=====

Install ``qmk-hid-identify`` to ``/usr/local/bin`` and then add the
`udev.rules <udev.rules>`_ to ``/etc/udev/rules.d/qmk-hid-identify.rules``
to automatically run for every device that is connected.
